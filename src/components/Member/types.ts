import { TouchableOpacityProps } from "react-native";

export type dataProps = {
  id: string
  username: string
  avatar_url: string
  status: string
}
export type MemberProps = {
  data: dataProps
}
